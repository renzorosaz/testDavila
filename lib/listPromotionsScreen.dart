import 'package:flutter/material.dart';

class ListPromotionsScreen extends StatelessWidget {
  const ListPromotionsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
